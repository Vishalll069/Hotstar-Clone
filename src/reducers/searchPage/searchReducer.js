import { SEARCH_RES } from "../../constants/searchPage/ActionTypes";

const initialSearch = {
    searchRes : [],    
}


const searchReducer =(state = initialSearch, action)=>{
    const {type, payload} = action;
    switch(type){
        case SEARCH_RES:
    }


}